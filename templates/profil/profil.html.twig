{% extends 'base.html.twig' %}

{% block title %}Hello {{ user.pseudo }}{% endblock %}

{% block body %}


<h3>{{ user.pseudo }}</h3>
<br>
<h3>{{ user.email }}</h3>
<br>
<h3>{{ user.nom }}</h3>
<br>
<h3>{{ user.prenom }}</h3>
<br>
<br>
<a href="{{ path("accueil") }}">Accueil</a>
<br>
<a href="{{ path("update", {"pseudo" : user.pseudo}) }}">modifier le profil</a>
<br>
<a href="{{ path("allcollection", {"pseudo" : user.pseudo}) }}">Mes Collections</a>
<br>
<a href="{{ path("createcollection", {"pseudo" : user.pseudo}) }}">Créer une collection</a>



{% endblock %}
